const chalk = require('chalk');
const { EOL } = require('os');

module.exports = {
	greenLine: chalk.green('------------------------------------'),
	redLine: chalk.red('------------------------------------'),
	EOL,
};
